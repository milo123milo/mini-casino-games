// slotLogic.h
#ifndef SLOTLOGIC_H
#define SLOTLOGIC_H

#include "lvgl.h"
#include "iconRandomizer.h"  // Include the header where SELECTED_COUNT is defined

int process_slot_result(lv_obj_t *selected_icons[SELECTED_COUNT]);

#endif 